# Never fill production relations with placeholders

Production Scientific Edges require real Version-specific Evidence, and V0.1 does not require every relation enum to have a positive production example. Missing relations use isolated synthetic fixtures for validation tests, while production counts of zero remain valid and are reported explicitly.
