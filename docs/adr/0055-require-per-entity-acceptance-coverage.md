# Require per-entity acceptance coverage

Every major domain structure must satisfy Stored, Validated, and Rendered through real fixture-based acceptance checks. Rendering may occur in Reader View, Provenance Detail, or Validation Report according to the structure's audience rather than forcing all governance metadata into the reading surface.
