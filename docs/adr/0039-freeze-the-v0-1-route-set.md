# Freeze the V0.1 route set

V0.1 implements `/`, `/papers`, `/papers/[work-id]`, `/research-lines/[id]`, and `/learning-paths/[id]`, using immutable Work ID as the canonical Paper route key. A standalone Research Map is deferred while the home page carries the initial map view.
