# Use Version-bound typed Evidence locators

Every Evidence record references a concrete Version and typed locator components. An excerpt is only a verification aid, never a provenance substitute, and a checksum is recorded only when the referenced source snapshot has actually been preserved.
