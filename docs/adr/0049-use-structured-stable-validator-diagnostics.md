# Use structured stable validator diagnostics

Validator diagnostics carry severity, code, dataset, file, record ID, field path, message, and related IDs. Diagnostic field paths and bibliographic field-provenance paths share record-relative RFC 6901 JSON Pointer syntax; codes and relevant structured context form the stable test contract, while human-readable messages may improve without breaking tests.
