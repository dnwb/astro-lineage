# Freeze the V0.1 verification command contract

V0.1 provides `npm run validate`, `npm run test`, `npm run check`, `npm run build`, and `npm run verify`. Direct production builds remain validation-gated, while `npm run verify` runs validation, tests, static checks, and production build as the single completion proof.
