# Use an independent project Git boundary

`/home/long/axvdaily` will be initialized as an independent Git repository, and the parent `/home/long` repository is not the project's version-control boundary. Local repository initialization and creation of an external remote remain separate actions.
