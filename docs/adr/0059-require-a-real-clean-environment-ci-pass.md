# Require a real clean-environment CI pass

V0.1 completion requires both local `npm run verify = 0` and a passing remote CI run in a clean environment. CI independently reproduces verification but does not imply or trigger external site deployment.
