# Allow multiple independent Edges per Work pair

A pair of Works may have multiple Edges only when each represents a distinct Scientific Delta with its own reason and Evidence. The graph does not persist weaker relationships that are naturally entailed by a more specific Edge, preventing redundant labels from overstating graph density.
