# Use axis defaults with reasoned risk escalation

Each Ontology Axis defines a default Interpretive Risk that controls Annotation evidence and review requirements. A specific Annotation may be escalated with a recorded reason but may not be silently downgraded, balancing consistent defaults with genuinely interpretation-heavy cases.
