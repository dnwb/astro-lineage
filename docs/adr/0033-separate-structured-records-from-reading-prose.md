# Separate structured records from reading prose

YAML is canonical for structured domain records, while Markdown is canonical for human-facing editorial reading prose in V0.1. The two sources join through immutable Work IDs, preserving static, human-editable content without forcing Evidence, review history, and Physical Account structure into oversized front matter; MDX remains deferred until a demonstrated content requirement exists.
