# Defer external deployment

V0.1 requires successful local and CI production verification but does not require an external deployment. Hosting platform selection and production authorization remain a later decision, keeping the milestone focused on model and architecture evidence.
