# Preserve Version-specific Statement Attestations

Scientific Statement identity remains Work-local while concrete Versions provide historical Attestations. A semantic change creates a new Statement and preserves prior Attestations rather than overwriting them, allowing the model to retain how a Work's claims, assumptions, predictions, or results evolved across Versions.
