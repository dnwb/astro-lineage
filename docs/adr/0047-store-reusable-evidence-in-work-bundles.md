# Store reusable Evidence in Work bundles

Each Work bundle owns an `evidence.yaml` registry whose records reference Versions from the same Work. Annotations, Scientific Statements, Causal Links, and Scientific Edges reuse Evidence IDs instead of duplicating locator data, preventing provenance drift across assertions.
