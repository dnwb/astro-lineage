# Split Work bundles by concern

Each Work bundle always contains `work.yaml`, `versions.yaml`, `evidence.yaml`, `annotations.yaml`, `statements.yaml`, `physical-account.yaml`, and `reading.md`. Empty concerns use explicit valid empty collections and missing files are structural errors; records remain grouped by concern rather than split one-per-file, while Scientific Edges retain one-file-per-Edge storage.
